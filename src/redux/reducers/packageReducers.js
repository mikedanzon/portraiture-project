const initialState = {
  isLoading: false,
  data: null,
  error: null,
};

const packageReducers = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default packageReducers;
