const initialState = {
  isLoading: false,
  data: null,
  error: null,
};

const projectReducers = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default projectReducers;

